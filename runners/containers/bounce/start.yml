group:
  commands:
  - if:
      conditional:
        not:
          group:
            commands:
            - container:
                status:
                  running:
                    inventory: bounce_container_name
      then:
        group:
          commands:
          - container:
              start:
                name:
                  inventory: bounce_container_name
