group:
  commands:
  - if:
      conditional:
        not:
          group:
            commands:
            - container:
                status:
                  running:
                    inventory: controller_container_name
      then:
        group:
          commands:
          - container:
              start:
                name:
                  inventory: controller_container_name
